---
layout: base
---

<div class="wrapper">
  <div class="container">
    <h1>Hamon</h1>
    
    
    
    <dl>
      <dt><a href="{{ site.baseurl | append: '/about/' }}">About</a></dt>
      <dt><a href="{{ site.baseurl | append: '/work/' }}">Work</a></dt>
      <dt><a href="{{ site.baseurl | append: '/lycaeum/' }}">The Lycӕum</a></dt>
      {% for collection in site.collections %}
        <dt>
          {% assign slug = collection.title | slugify %}
          <a href="{{ site.baseurl | append: '/' | append: slug }}">
            {{ collection.title }}
          </a>
        </dt>
        <dd class="prose">
          {% if collection.output %}
            <ul>
              {% for doc in collection.docs %}
                <li>
                  <a href="{{ site.baseurl | append: doc.url }}">
                    {{ doc.title }}
                  </a>
                </li>
              {% endfor %}
            </ul>
          {% else %}
            <dl>
              {% for doc in collection.docs %}
                <dt>{{ doc.title }}</dt>
                <dd>{{ doc.content }}</dd>
              {% endfor %}
            </dl>
          {% endif %}
        </dd>
      {% endfor %}
    </dl>
  </div>
</div>

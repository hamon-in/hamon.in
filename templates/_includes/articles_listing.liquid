<ul>
  {% for article in include.articles reversed %}
    {% assign link = site.baseurl | append: article.url %}
    {% unless article.id == include.exclude_id %}
      <li class="embed-spacer">
        <h3 class="size-regular title-two brand-three">
          <a href="{{ link }}">
            {{ article.title | smartify }}
          </a>
        </h3>
        <div class="prose">
          {{ article.brief | markdownify }}
          <p class="collapse">
            {% if article.author %}
              <i class="size-small">
                by
                {{ article.author.name }}
                ·
              </i>
            {% endif %}
            <a class="silent" href="{{ link }}">
              <time class="title-fineprint title-type size-xsmall">
                {{ article.date | date: '%b %-d, %Y' }}
              </time>
            </a>
          </p>
        </div>
      </li>
    {% endunless %}
  {% endfor %}
</ul>
